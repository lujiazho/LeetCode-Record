///////////////////////////////////////////////////////////////////////////////////////////////
// interesting
////////////////
// Time Complexity: O(1)
// Space Complexity: O(1)
///////////////////////////////////////////////////////////////////////////////////////////////
class Solution {
public:
    void deleteNode(ListNode* node) {
        // for c++, *node is actually a reference, so we can directly change it
        *node = *node->next;
    }
};